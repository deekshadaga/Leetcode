class Solution {
public:
    bool isSubsequence(string s, string t) {
        int j=0,i=0;
        while(i<=t.length()){
            if(j==s.length()) return true;
            if(s[j]==t[i]) j++;
            i++;
        }
        return false;
    }
};
